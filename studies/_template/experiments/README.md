# Experiments

This directory defines what to run and how to evaluate it. Store the run matrix,
controlled factors, seed schedule, reproducible commands, launch scripts, and small
analysis programs here.

Do not store generated outputs here. Experiment outputs belong in `../results/`.
Algorithm implementations and shared configurations remain in their existing URB
locations.
